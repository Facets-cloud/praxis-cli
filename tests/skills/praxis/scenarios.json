[
  {"id":"shared-identity","prompt":"I switched praxis profiles use staging. Does Raptor move too, and which credential store wins inside a project?","checks":["shared PAT default can move Raptor","project .facets wins","explicit precedence","no credential dump"]},
  {"id":"mcp-output","prompt":"Build a parser for Praxis MCP JSON/text/multipart/errors; what happens when --body and --arg are both present?","checks":["body replaces args","successful JSON unwrap only","preserve text/multipart/error envelopes","capture exit and domain result"]},
  {"id":"duty-trigger","prompt":"Run a paused duty once. Is praxis duty run ID the trigger?","checks":["native run reads run_id","MCP trigger schedule_id","no resume needed","exact returned run tracked"]},
  {"id":"blueprint-import-boundary","prompt":"Bulk edit blueprint Git values, set prod overrides and adopt an existing DB without cloud changes. Choose routes and ownership boundaries.","checks":["Raptor canonical routes","Git versus CP overrides","plan-first zero-change import","no data copy implied","no prefixed duplicate recipe"]},
  {"id":"catalog-read-build","prompt":"Query an existing catalog, then publish a new catalog. Which checkout/build/sync operations are necessary? Build output is /tmp/build/member/api/graphify-out/graph.json.","checks":["MCP reader no local sync required","builder separate","publish /tmp/build root","manifest push needs catalog","portable identity"]},
  {"id":"cache-cutover","prompt":"Redis sessions need moving. The bundled monitor reports VERIFY CLEAN. Can we cut over? What evidence remains?","checks":["authoritative data","type-size sample is insufficient","values TTL shard/db and lag independent","freeze and final verify","infra adoption not copy"]},
  {"id":"gateway-effects","prompt":"Are New Relic calls, Facets account sync, and SSM all read-only because they go through Praxis?","checks":["New Relic mutable verbs","integration sync writes","SSM send-command effects","no extra write authority"]},
  {"id":"dag-ownership","prompt":"Drive a run unattended. One node is manual, another lost its lease; the last complete_node response timed out. What's next?","checks":["preserve manual node","inspect ownership","reconcile completion","independent judge","human deep link"]},
  {"id":"document-artifact","prompt":"Publish revised audit evidence, then hand it to a DAG judge and a colleague. Which identity, hash and URL should we retain?","checks":["same session/name versions","exact stored sha","artifact identity ref","durable URL not session latest","no CI artifact mixup"]},
  {"id":"component-contract","prompt":"Build an environment-scoped panel that displays pod health. Some responses are absent; the env depends on a base cluster. Which parts own building and registration?","checks":["Praxis build Raptor registration","five states","no redundant scope picker","same-origin session","base tunnel and timeout","explorer queue","IIFE and shadow theme"]},
  {"id":"secret-verification","prompt":"The operator has secretVersionAdder and the helper says OK after copying to latest. Does that prove byte-identical ESO secrets?","checks":["needs describe/read-back","latest concurrency limit","short digest not strict byte proof","ESO wiring separate","no value disclosure"]},
  {"id":"retirement-provenance","prompt":"A machine has a modified praxis-cloud-operations folder, an organization skill with the same name, and a Raptor binary without its skill. Can refresh delete all praxis-*?","checks":["no glob delete","scope provenance","modified backup/preserve","missing Raptor retains legacy","active root only"]},
  {"id":"waste-coverage","prompt":"One region shows low CPU and no DB connections. Another account is unauthorized. Report total safe-to-delete savings.","checks":["partial coverage explicit","no access != zero waste","DR seasonal retention","no safe-to-delete claim","current price estimate not invented exact"]}
]
